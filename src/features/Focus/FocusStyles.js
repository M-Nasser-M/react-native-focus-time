import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 0.5,
    padding: 16,
    justifyContent: 'center',
  },
  titleText: {
    fontSize: 24,
    color: 'white',
  },
  textInput: {
    flex: 1,
    marginRight: 10,
  },
  buttonContainer: {
    flexDirection: 'row',
    marginTop: 15,
    alignItems: 'center',
  },
});
